{
  "$id": "wallet.schema.json",
  "title": "Wallet",
  "type": "object",
  "properties": {
    "playerId": { "type": "string", "minLength": 1 },
    "credits": { "type": "number", "minimum": 0 }
  },
  "required": ["playerId", "credits"],
  "additionalProperties": false
}
