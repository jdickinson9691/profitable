{
  "$id": "scanner.schema.json",
  "title": "Scanner",
  "type": "object",
  "properties": {
    "id": { "type": "string", "minLength": 1 },
    "tier": { "$ref": "tierColor.schema.json" },
    "ownerId": { "type": "string", "minLength": 1 }
  },
  "required": ["id", "tier", "ownerId"],
  "additionalProperties": false
}
