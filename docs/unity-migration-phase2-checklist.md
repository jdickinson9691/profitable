# Unity Migration — Phase 2 Checklist

**What this is:** a concrete, actionable checklist for getting Profitable's Alpha-milestone functionality running in Unity, beyond Migration Phase 1's MVP loop (gather → refine → craft). "Alpha Phase 2" here means `docs/profitable-unity-migration-gdd.md`'s **Migration Phase 2+** — the GDD explicitly scopes that as "future GDDs, not scoped here" (§2), and `docs/functional-agents/build.md` is the *meta-agent contract* that computes readiness and generates agent rosters, but is itself a spec for an orchestrator, not a followable task list. This file is that followable task list, recomputed fresh against today's actual code state (2026-08-03) rather than `build.md`'s own example table, which is already stale in several places (see below).

**How to use this doc:** work top to bottom. Each sub-phase below is independently portable once its prerequisites are checked — they don't have to run in the listed order, but *within* a sub-phase the role order (Schema → Simulation Core → Parity Validation → Infrastructure Adapters → Presentation → Phase Integration) is a real dependency chain, same as Migration Phase 1 proved out. Do not skip Parity Validation for any sub-phase — see `profitable-unity-migration-gdd.md` §6.

**On agent numbering:** this file deliberately does **not** pre-assign specific `docs/agents/agent-N-*` numbers to any task below. `build.md` itself found a real numbering collision once already (Agent 37 was claimed by Planet Ownership Core while a Phase 2 roster still assumed 36 was the ceiling) — precisely the "never trust a cached snapshot" failure mode this project explicitly guards against. When a task below is actually started, check `ls docs/agents/` fresh, use the next free number, and write that task's own contract file at that time.

**Retroactive removal (2026-08-04, after the post-completion audit below):** Citadels (Sub-Phase E) was fully built and ported, then cut from alpha scope entirely — a scope reduction, not a defect found in this migration. Sub-Phase E's roster below is left as the historical build record of what was ported and how; see its own retroactive-removal note (after its role list) for what was subsequently removed on both the TS and C# sides, and `docs/functional-agents/planet-ownership.md`'s own retroactive note for the full account.

---

## 0. Prerequisites — do these before starting any sub-phase below

- [x] **Write `docs/functional-agents/crew.md`.** Done — the core Crew system (hire/wage/upkeep/attrition — `src/crew/hireCrew.ts`, `assignToCraft.ts`, `payUpkeep.ts`, `checkAttrition.ts`, `dismissCrew.ts`, `purchaseCapacity.ts`, `refreshCrewPool.ts`, `resolveBackgroundCrafting.ts`) now has a consolidated contract (`docs/functional-agents/crew.md`), matching every other functional area's shape. `BACKGROUND_IDLE_OUTPUT_RATE` is now resolved (0.5/hour) with real inventory consumption wired into `CrewScene`'s `> Check Background` action. `CREW_POOL_REFRESH_INTERVAL_HOURS` now correctly re-rolls a stale pool (`getCrewPool()`) — the same fix landed for the shipyard/scanner pools' identical gap (`getShipyardPool()`/`getScannerPool()`, `travel.md`). No known Crew gaps remain.
- [ ] **Confirm the Phase 1 baseline is still green** before adding anything on top of it: `cd unity && dotnet build && dotnet test`, and regenerate the parity corpus (`npm run parity` from repo root) to confirm it still matches `unity/ProfitableCore.Tests/Parity`'s expectations. A silent Phase 1 regression would poison every sub-phase built on top of it.
- [ ] **Re-run the readiness table below against `docs/functional-agents/*.md`'s live Status lines** if any time has passed since 2026-08-03 — this file decays the same way `build.md` warns its own cached table does. Do not trust this snapshot without re-checking.

---

## 1. Readiness table (recomputed 2026-08-03 — corrects `build.md`'s own example table)

| Area | `docs/functional-agents/` file(s) | TS status today | Change vs. `build.md`'s table |
|---|---|---|---|
| Galaxy generation | `galaxy.md` | Fully as-built | Unchanged — already correct |
| Planet generation | `planet.md` | Fully as-built | Unchanged |
| Mining | `mining.md` | Fully as-built | Unchanged |
| Refining | `refining.md` | Fully as-built | Unchanged |
| Crafting | `crafting.md` | Fully as-built | Unchanged |
| Recipes/Schematics | `recipes-schematics.md` | Fully as-built | Unchanged |
| Planetary Markets | `planetary-markets.md` | Fully as-built | Unchanged |
| Galactic Market | `galactic-market.md` | Fully as-built | Unchanged |
| **Travel (incl. Scanner)** | `travel.md` | **Fully as-built** — Ship Fuel/Cargo Hold Capacity preconditions on `initiateVoyage()` now built | **Corrected: was "Partially," now Yes** |
| **Ship** | `ship.md` | **Fully built** — all 5 Crew Role effects built including Systems Engineer's repair (task #89) and `> Unassign`; the starting-ship fuel bootstrap was removed as dead code, not left as a gap. Only remaining polish item: one unverified numeric claim (a missing regression test, not missing behavior) | **Corrected: was "No," now Yes** |
| Encounters/Combat | `encounters-combat.md` | Fully as-built | Unchanged |
| **Planet Ownership** | `planet-ownership.md` | **Colonist-Driven Production fully as-built.** Citadel claim/build, refuel discount, and depth-scaled repair were also fully built at the time this table was last accurate, then retroactively cut from alpha scope (2026-08-04, see the note above and Sub-Phase E below) — a scope reduction, not a gap | **Corrected: was "Partially" with 1 gap, now Yes; Citadels portion later removed** |
| Crew (core) | `crew.md` — **now exists, doc gap closed (§0)** | Fully as-built (`agent-16-crew-core.md`); `BACKGROUND_IDLE_OUTPUT_RATE` resolved (0.5/hour); `CREW_POOL_REFRESH_INTERVAL_HOURS` now correctly re-rolls a stale pool. No known gaps remain | **Yes** |
| Universe | `universe.md` | Fully designed, zero code | **No** — deliberately unscheduled, excluded entirely, not just blocked |

**Bottom line: everything except Universe is TS-ready simultaneously.** What was originally sequenced as separate Migration Phases 2 (Galaxy/Planet) → 3 (Trading) → 4 (Crew) → 5 (Ships/Travel) → Combat is, as of today, one large bundle of ready work with zero remaining TS-side blockers — Crew's documentation gap and Planet Ownership's cargo-storage question (the two blockers noted in earlier revisions of this table) are both closed. The sub-phases below stay separated to mirror the original TypeScript Creation Order and keep each Parity Validation pass scoped and reviewable — not because they're blocked on each other.

---

## Sub-Phase A: Galaxy, Planet, Mining

**Covers:** `galaxy.md`, `planet.md`, `mining.md`. Fully as-built, zero blockers. **All six roles are done** (agents 38-42) — Sub-Phase A is complete.

- [x] **Schema** (`agent-38-unity-galaxy-planet-schema.md`). Extended `Planet.cs` with `PlanetType`/`Tier`/`Position`/`SpecialtyResourceId`/`Discovered`/`ResourceQualities`/`ColonistCount`/`CitadelLevel`/`OwnedByPlayerId`; new `PlanetType` enum, `PlanetPosition`, `Galaxy` types; new `PlanetTierModifierTable`, `PlanetTypeEligibilityTable`, `ResourceSubsetPercentageTable`, `PlanetResourceCycleConstants`, `GalaxyGenerationConstants` (`PositionRange`, load-bearing for the fuel-capacity regression test), and `PlanetOwnershipConstants` (just `MinimumColonistsToProduce` — Sub-Phase A's one hard dependency on Sub-Phase E's domain).
- [x] **Simulation Core** (`agent-39-unity-galaxy-planet-simulation-core.md`). Ported `SeededRandom` (bit-for-bit — verified against a real recorded TS sequence before building anything on top of it), `GalaxyGenerator`, `PlanetGenerator`, `ResourceSubsetSelector` (including the real `ItemTier == 1` bug-fix), `PlanetQualityRoller`, `PlanetResourceCycle` (cycle index, `GenerateResourcesForCycle`, the tutorial guarantee, `GetCurrentPlanetResources`). Also ported `AggregateTierResolver` (`src/simulation/aggregateTier.ts`) as a necessary completion — needed by the tutorial guarantee, wasn't in Migration Phase 1's scope. **Found and fixed a real rounding-semantics gap querying this exact risk class**: JS's `Math.round()` rounds every exact `.5` toward `+Infinity` regardless of sign, but the existing `JsMath.Round` (Phase 1) uses `AwayFromZero`, which is only equivalent for `Round`'s original always-positive quality domain — planet positions are signed (`-PositionRange..+PositionRange`), so `AwayFromZero` would round a negative `.5` the wrong direction. Added `JsMath.RoundSigned` (`Math.Floor(x + 0.5)`, the actual ECMAScript algorithm for any sign) rather than reusing or modifying `Round`.
- [x] **Parity Validation** (`agent-40-unity-galaxy-planet-parity-validation.md`). Extended `scripts/parityHarness.ts` with `galaxyCases` (3 seeds × 2 planet counts, including real 50-planet scale), `planetResourceCycleCases` (3 subjects × 4 cycle indices, proving cross-cycle divergence), `gcprCases` (5 scenarios: colonist-gate boundaries + tutorial guarantee at 2 different cycles) — all generated against the **real** `content/*.json` catalog (60 resources), not a synthetic fixture set. **541/541 C# tests pass** (504 Phase 1 + 23 new parity cases + 14 direct unit tests) — the C# port agrees with the real TypeScript output end-to-end, not just per-function.
- [x] **Infrastructure Adapters.** Confirmed not needed — no new adapter type beyond Phase 1's `SaveSystem`/`AudioManager`.
- [x] **Presentation** (`agent-41-unity-galaxy-planet-presentation.md`). `GalaxyState.cs` (new, session-only, fixed seed `"unity-mvp-galaxy"`, 50 planets — matches real alpha scale). Rewrote `MapPanel.cs` to show the real generated galaxy (replacing the static "Delta Rigelus" label) and `GatherPanel.cs` to read `PlanetResourceCycle.GetCurrentPlanetResources()` once per visit instead of rolling `QualityRoller.RollQuality()` fresh per click, matching `mining.md`'s own architectural shift. Starting planet's `Discovered`/`ColonistCount` minimally overridden in-memory (not the full persisted ownership/discovery machinery — that's Sub-Phase D/E's job).
- [x] **Phase Integration** (`agent-42-unity-galaxy-planet-mining-integration.md`). Confirmed the real generated galaxy drives the Map/Gather panels and the existing gather → refine → craft loop still completes end-to-end — reused `FullLoopClickThroughTest.cs` (Migration Phase 1's own real-click-through test) unmodified as Sub-Phase A's own integration proof, since the tutorial guarantee keeps its hardcoded button names valid regardless of the real galaxy's roll. Full sweep: `dotnet test` 541/541, Unity EditMode 28/28, Unity PlayMode 2/2, `npm test` 687/687 (TS source unaffected).

**Retroactive amendment (2026-08-05): Per-Resource Quantity Caps ported alongside the TS source, in the same session that built it, not as a separate follow-up sub-phase.** `ResourceQuantityCapTable.cs` (mirrors `resourceQuantityCap.ts`), `PlanetResourceCycle.ResourcesForCycle.ResourceQuantityCaps` (mirrors the TS field, same tutorial-guarantee exemption), a new pure `ResourceDepletion.cs` (mirrors `resourceDepletion.ts`), a new persisted side-table `ResourceDepletionState.cs` (mirrors `resourceDepletionState.ts`, same `PlanetOwnershipState.cs`-derived pattern), and `GatherPanel.cs`'s gather-button row rebuilt via `UiFactory.ClearChildren` to show a depleted resource as a plain label instead of a button. Parity corpus regenerated and extended (`resourceQuantityCaps` added to `ExpectedResourcesForCycle`'s DTO and both parity assertions); `dotnet test` reconfirmed at 742/742. **A real gap in this build environment, not the feature itself, found and worked around:** `Assets/Plugins/ProfitableCore.dll` is a checked-in prebuilt DLL, not a live project reference (`unity/README.md`'s own documented "not source-embedded" design) — the Unity `Runtime`/`Tests.EditMode` `.csproj` files also use explicit `<Compile Include>` file lists rather than a wildcard glob, normally kept in sync by the Unity Editor reopening the project, unavailable in this environment. Both were updated by hand (`dotnet build -c Release` + copy to `Assets/Plugins/`, plus a new `<Compile Include="Assets\Scripts\Content\ResourceDepletionState.cs" />` entry) and reverified by a real `dotnet build` of `Profitable.Unity.Runtime.csproj`/`Profitable.Unity.Tests.EditMode.csproj` (both succeed) — the closest thing to a real Unity-side compile check achievable without the Editor itself. Unity EditMode/PlayMode were not independently re-run (no Unity Editor available here, same limitation as every other Unity-side change this session).

---

## Sub-Phase B: Trading (Planetary + Galactic Market)

**Covers:** `planetary-markets.md`, `galactic-market.md`. Fully as-built, zero blockers. Two files, same core (`src/trading/`), different presentation screens — port together, present separately, per those files' own split rationale. **All six roles are done** (agents 43-47) — Sub-Phase B is complete.

- [x] **Schema** (`agent-43-unity-trading-schema.md`). Ported `Listing`, `PlanetMarketState`, `Wallet`, `MarketLocation` (sealed-class union, `GlobalMarketLocation`/`PlanetMarketLocation`), `PurchaseResult` (sealed-class union, `PurchaseSucceeded`/`PurchaseRejected`), `SellToMarketResult`, `SellToGlobalMarketResult`, `ListingExpiryResult`/`ReturnAction`, `ItemBasePrice`, `PlanetMarketPreference`, `TradeDirection`; new `TradingConfig` with all 15 tunables (mutable `public static` properties for the debug-tunable ones — the idiomatic C# equivalent of the TS `let`+setter pattern, established here for the first time this migration needs it).
- [x] **Simulation Core** (`agent-44-unity-trading-simulation-core.md`). Ported `createListing.ts` (as `ListingFactory`), `purchaseListing.ts`, `drift.ts`, `season.ts`, `emergency.ts`, `expireListings.ts`, `globalPrice.ts`, `sellToMarket.ts`, `sellToGlobalMarket.ts`, `loadTradingContent.ts` (as `TradingContentLoader`, same hand-written-validation pattern as `ContentLoader`, no AJV/JSON-Schema dependency).
- [x] **Parity Validation** (`agent-45-unity-trading-parity-validation.md`). 30 new corpus cases across all 10 functions against the real content catalog, including a search-found (not hand-picked) triggered-emergency case and two drift cases sized to hit the floor/ceiling clamp. **591/591 C# tests pass** (541 through Sub-Phase A + 30 parity + 20 direct unit tests covering error paths real content can't reach).
- [x] **Infrastructure Adapters.** Confirmed not needed — same as Sub-Phase A.
- [x] **Presentation** (`agent-46-unity-trading-presentation.md`). `MarketState.cs` (new, session-only, single starting-planet market state per item) + `MarketPanel.cs` (new, fixed 5-resource × 2-destination sell button grid) wired into `MvpLoopBootstrap` as a fifth panel. Scoped to the Trading Counterparty instant-sell functions only (`SellToMarket`/`SellToGlobalMarket`) — the full Listing create/browse/purchase flow needs a second player this single-player MVP doesn't have, so it's explicitly deferred, not silently skipped.
- [x] **Phase Integration** (`agent-47-unity-trading-integration.md`). Confirmed the Market panel drives real gather → refine → craft → sell end-to-end, and Sub-Phase A's own `FullLoopClickThroughTest.cs`/`MvpLoopSceneSmokeTest.cs` pass unmodified with the fifth panel present (regression proof). Full sweep: `dotnet test` 591/591, Unity EditMode 33/33, Unity PlayMode 2/2, `npm test` 687/687.

---

## Sub-Phase C: Crew (core hire/wage/upkeep/attrition)

**No longer blocked — `docs/functional-agents/crew.md` now exists (§0).** The TS side itself is fully as-built (`docs/agents/agent-16-crew-core.md`, consolidated in `crew.md`). **All six roles are done** (agents 48-52) — Sub-Phase C is complete.

- [x] **Schema** (`agent-48-unity-crew-schema.md`). Ported `CrewMember` (+ `CrewStatus`), `CrewCandidate`, `PlanetCrewPool`, `CrewCapacity`, `CraftAction`, `HireResult`/`DismissResult`/`PaymentResult`/`AttritionResult`/`PurchaseCapacityResult`/`AssignResult`/`BackgroundResult`, and `ShipCrewRole` (scoped early — Sub-Phase C's own `CrewMember.ShipRole` field needs it, though the role-assignment logic itself is Sub-Phase D's job). New `CrewConfig` with all tunables, including the two tier-keyed tables as plain mutable `Dictionary<TierColor, double>`.
- [x] **Simulation Core** (`agent-49-unity-crew-simulation-core.md`). Ported `hireCrew.ts`, `assignToCraft.ts`, `payUpkeep.ts`, `checkAttrition.ts`, `dismissCrew.ts`, `purchaseCapacity.ts`, `refreshCrewPool.ts`, `resolveBackgroundCrafting.ts` (including its `maxUnits` cap). **Found and handled a real default-parameter-semantics gap**: TypeScript's `backgroundRate: number | null = BACKGROUND_IDLE_OUTPUT_RATE` default is evaluated live per call, which C#'s compile-time-constant default parameters can't replicate directly — solved with two overloads (one reading `CrewConfig.BackgroundIdleOutputRate` fresh, forwarding to the full one) rather than silently freezing the default at compile time.
- [x] **Parity Validation** (`agent-50-unity-crew-parity-validation.md`). 24 new corpus cases across all 8 functions, including the omitted-vs-explicit-null `backgroundRate` distinction and the grace-period boundary. **624/624 C# tests pass** (591 through Sub-Phase B + 24 parity + 9 direct unit tests).
- [x] **Infrastructure Adapters.** Confirmed not needed — same as Sub-Phases A/B.
- [x] **Presentation** (`agent-51-unity-crew-presentation.md`). `CrewState.cs` (new, session-only, shares `MarketState.Wallet`) + `CrewPanel.cs` (new, hire/pay-upkeep/dismiss/assign-to-craft/purchase-capacity) wired into `MvpLoopBootstrap` as a sixth panel. First dynamic-rebuild UI this migration needed (`UiFactory.ClearChildren`, new) — a crew roster/hire pool genuinely grows and shrinks at runtime, unlike every prior panel's fixed button set. Pool staleness re-roll ported alongside (mirrors this session's own TypeScript fix for dead crew/shipyard/scanner pools).
- [x] **Phase Integration** (`agent-52-unity-crew-integration.md`). Confirmed assigning crew to a craft action genuinely drives `Crafter.Craft`'s crafterTier argument from the crew member's own tier (not a hardcoded value), and Sub-Phase A/B's own `FullLoopClickThroughTest.cs`/`MvpLoopSceneSmokeTest.cs` pass unmodified with the sixth panel present. Full sweep: `dotnet test` 624/624, Unity EditMode 40/40, Unity PlayMode 2/2, `npm test` 687/687.

---

## Sub-Phase D: Ships & Travel (incl. Scanner)

**Covers:** `ship.md`, `travel.md`. Both effectively fully as-built as of today. **All six roles are done** (agents 53-57) — Sub-Phase D is complete.

- [x] **Honest pre-check.** Confirmed: `resolveComponentRepair()`'s manual `> Check Repair` caller is a deliberate design choice carried forward faithfully (Agent 56's own Presentation job, not "fixed" into an automatic poll). `STARTING_SHIP_FUEL_CAPACITY` stays removed; the fuel-capacity regression test's underlying invariant is exercised by Agent 55's own parity corpus against real galaxy positions.
- [x] **Schema** (`agent-53-unity-ships-travel-schema.md`). Ported `Ship`/`ShipComponent`/`ShipComponentSlots`/`ComponentCategory`/`Voyage`/`VoyageCargoItem`/`Scanner`/`ScannerPool`/`ShipCandidate`/`ShipyardPool`/`ComponentRecipe` and every result union, plus `ShipsAndTravelConfig` (~30 tunables). **Also ported Encounter/Combat schema here** (`EncounterResult`, `CombatEncounter`, `CombatResolution`, `EncounterResolution`) even though Sub-Phase F's own checklist entry names these — `resolveEncounters`/`resolveCombatChoice`/`initiateCombat` live in `src/ships/` and are assigned to this sub-phase's own Simulation Core row, so the types must exist here first; Sub-Phase F's own Schema role is now "confirm/extend," not "create." Extended `PlanetOwnershipConstants` (Sub-Phase A) with `CitadelLevelBenefits`.
- [x] **Simulation Core** (`agent-54-unity-ships-travel-simulation-core.md`). Ported all 21 `src/ships/*.ts` functions plus `loadShipsContent.ts`. **Found and fixed a real precision bug via a failing parity test**: `Voyage.ArrivesAt` (derived from floating-point travel-time math) was initially typed `long`, silently truncating every voyage's fractional arrival millisecond — corrected to `double`, matching the TypeScript source's plain `number` type exactly.
- [x] **Parity Validation** (`agent-55-unity-ships-travel-parity-validation.md`). 21 new corpus sections (~65 cases) backed by a real generated galaxy's actual planet positions, not synthetic coordinates. Two real serialization gaps found and fixed via failing tests (missing `Planet.OwnedByPlayerId`/`CitadelLevel` on the DTO; missing origin/current planet data entirely on `resolveCombatChoiceCases`, which the C# side needs to reconstruct the real retreat-voyage call). **716/716 C# tests pass** (651 through Sub-Phase C + 65 parity + 27 direct unit tests).
- [x] **Infrastructure Adapters.** Confirmed not needed — same as Sub-Phases A/B/C.
- [x] **Presentation** (`agent-56-unity-ships-travel-presentation.md`). `ShipsState.cs` (new, session-only, shares `MarketState.Wallet`) + `ShipsPanel.cs` (new, purchase/refuel/check-repair/travel/resolve-arrival) wired into `MvpLoopBootstrap` as a seventh panel. `GalaxyState.SecondaryDestinationPlanet` added as a real travel destination (mirrors `galaxyState.ts`'s own `secondaryDiscoveredPlanet`). Scoped to the core purchase→refuel→repair→travel→arrival loop only — ship-crew-role assignment, scanner purchase/scan, and combat/encounter resolution UI are all explicitly deferred (the last to Sub-Phase F's own Presentation agent).
- [x] **Phase Integration** (`agent-57-unity-ships-travel-integration.md`). Confirmed a real voyage can be initiated (real fuel deduction, real `CalculateTravelTime`-derived arrival time), correctly reports not-yet-arrived before its time, and repair genuinely reaches `ResolveComponentRepair`. Sub-Phases A/B/C's own `FullLoopClickThroughTest.cs`/`MvpLoopSceneSmokeTest.cs` pass unmodified with the seventh panel present. Full sweep: `dotnet test` 716/716, Unity EditMode 47/47, Unity PlayMode 2/2, `npm test` 687/687.

**Retroactive correction (2026-08-03):** a Unity migration parity audit re-checking every ported tier/breakpoint/threshold comparison for the `getTierColor()`/`penaltyCurve()` fractional-boundary risk class (`profitable-unity-migration-gdd.md` §6's own rule) found that `ResolveEncounters.ResolveHazard`'s `HazardFailureCostCurve` band lookup — ported here in Agent 54's Simulation Core role — was a byte-for-byte faithful port of a pre-existing, uncaught latent gap in the TypeScript source (`src/ships/resolveEncounters.ts`'s `resolveHazard`), not a bug introduced during translation. Full detail and fix: `profitable-travel-encounters-gdd.md`'s own retroactive correction note (same date), since the underlying bug lives in the original TS feature this sub-phase ported, not in anything Unity-migration-specific. Fixed in lockstep in both `src/ships/resolveEncounters.ts` and `unity/ProfitableCore/Simulation/ResolveEncounters.cs`; regression coverage added to both `tests/ships/resolveEncounters.test.ts` and `ShipsSimulationTests.cs`. No Sub-Phase D completion claim is retracted — every test that existed at the time passed, and still passes — but the audit's finding (a pre-existing TS gap the port faithfully carried over, undetected because neither suite exercised a non-Grey tier's contribution to this specific lookup) is documented rather than silently absorbed.

---

## Sub-Phase E: Planet Ownership (Colonists + Citadels — Citadels since removed, see note below)

**Covers:** `planet-ownership.md`. At the time this sub-phase was built, fully as-built including all 3 Citadel levels. **All six roles are done** (agents 58-62) — Sub-Phase E is complete; the role list below is the historical record of that work, left unmodified even though Citadels was subsequently removed (see the retroactive-removal note after the role list).

- [x] **Schema** (`agent-58-unity-planet-ownership-schema.md`). `colonistCount`/`citadelLevel`/`ownedByPlayerId` confirmed already present on `Planet.cs` (Sub-Phase A). `PlanetOwnershipEntry` (+ `Default()`), `TransportColonistsResult`/`ClaimPlanetResult`/`BuildCitadelResult` unions. `CitadelLevelBenefits` (started in Sub-Phase D with just `RefuelDiscountPercent`/`RepairEnabled`) extended with `ConstructionCostCredits`/`ConstructionMaterial`; `ColonistTransportCost` added.
- [x] **Simulation Core** (`agent-59-unity-planet-ownership-simulation-core.md`). Ported `transportColonists.ts`, `claimPlanet.ts`, `buildCitadel.ts`, `mergePlanetOwnership.ts`. Colonist-gathering-gate seam confirmed unchanged (still lives in `GetCurrentPlanetResources`, Sub-Phase A). `BuildCitadel` reuses Sub-Phase D's own `PlanetOwnershipConstants.CitadelLevelBenefits` dictionary directly, never a second lookup.
- [x] **Parity Validation** (`agent-60-unity-planet-ownership-parity-validation.md`). 12 new corpus cases across all 4 functions, including the real `iron-ingot` Citadel construction material. **743/743 C# tests pass** (716 through Sub-Phase D + 12 parity + 15 direct unit tests).
- [x] **Infrastructure Adapters.** None new — reuses Phase 1's `ISaveSystem`/`FileSaveSystem` directly (Agent 61's own Presentation role wires it, the first real `ISaveSystem` use in this migration's Presentation layer — every prior `*State.cs` class was deliberately session-only).
- [x] **Presentation** (`agent-61-unity-planet-ownership-presentation.md`). `PlanetOwnershipState.cs` (new) — the first REAL persistence in this migration's Presentation layer (`Adapters.FileSaveSystem`, not session-only). `GatherPanel.cs` extended with `Transport 5 Colonists`/`Claim Planet`/`Build Citadel` actions. `GalaxyState.cs`'s Sub-Phase A colonist-floor stopgap replaced with a real call into `PlanetOwnershipState.EnsureBootstrapColonization`.
- [x] **Phase Integration** (`agent-62-unity-planet-ownership-integration.md`). Confirmed persistence survives a simulated reload. **Found and fixed a real gap in Sub-Phase D's own `ShipsPanel`**: `RefuelShip`/`CheckRepair` always passed `null`/empty owned-crew, so no Citadel benefit could ever reach the real UI — invisible until real ownership state existed to make it observable. Fixed; a new test proves the discount is real. Full sweep: `dotnet test` 743/743, Unity EditMode 55/55, Unity PlayMode 2/2, `npm test` 687/687.

**Retroactive removal (2026-08-04, after the post-completion audit below): Citadels cut from alpha scope, in Unity as well as TypeScript.** Removed from the C# side: `ClaimPlanet.cs`/`BuildCitadel.cs` and their result schema, `CitadelLevel`/`OwnedByPlayerId` from `Planet.cs`/`PlanetOwnershipEntry.cs`/`MergePlanetOwnership.cs`, `PlanetOwnershipConstants.CitadelLevelBenefits`, `ShipsAndTravelConfig`'s `CitadelLevel2RepairRate`/`CitadelLevel3RepairRate`, the citadel-aware branches in `RefuelShip.cs`/`ResolveComponentRepair.cs` (both reverted to their pre-Citadels signatures), the `Claim Planet`/`Build Citadel` actions in `GatherPanel.cs`, and the corresponding parity/unit test cases across `ProfitableCore.Tests` and `ProfitableUnity/Assets/Tests`. `dotnet test` reconfirmed green post-removal (729/729). Colonist-Driven Production's own Unity port (this sub-phase's other half) is entirely unaffected. Not a defect found in this migration — a scope decision made afterward; see `docs/functional-agents/planet-ownership.md`'s own retroactive note for the full account. Unity EditMode/PlayMode counts were not independently re-run in this environment (no Unity Editor available here); expect a small reduction from the removed citadel-specific test cases in `GatherPanelTests.cs`/`ShipsPanelTests.cs`.

**Real regression this removal caused, Unity-only: `> Check Repair` is now a no-op for every ship.** Ship Crew Roles assignment UI (`> Assign`/`> Unassign` for Pilot/Combat Engineer/Science Officer/Systems Engineer/Crafter) was never built in Unity — Sub-Phase D's own Presentation entry above scopes it out explicitly ("ship-crew-role assignment... explicitly deferred"), and no code path anywhere in `unity/ProfitableUnity/` calls `AssignToShipRole`. Before this removal, Citadel repair was the one rate source `ShipsPanel.CheckRepair()` could still reach despite that gap (Sub-Phase E's own Phase Integration entry above fixed exactly this). With Citadel gone and crew-role assignment still unbuilt, `ComponentRepairResolver.ResolveComponentRepair()` now has zero eligible rate sources on every Unity call — it still stamps `Ship.LastRepairedAt`, but restores no durability. Documented in `ShipsPanel.cs`'s own `CheckRepair()` comment (`unity/ProfitableUnity/Assets/Scripts/UI/ShipsPanel.cs:280-289`) and `docs/functional-agents/planet-ownership.md`'s retroactive-removal note. **Not fixed here** — the fix is building Ship Crew Roles assignment UI in Unity, itself a real, separately-scoped piece of deferred work (see Sub-Phase D's Presentation entry), not a quick patch alongside this removal.

---

## Sub-Phase F: Encounters & Combat (incl. Travel Encounters)

**Covers:** `encounters-combat.md`. Fully as-built, zero blockers. Depends on Sub-Phase D (shares `resolveArrival()`'s entry point and ship/combat schema).

**Schema, Simulation Core, and Parity Validation are already done** — ported as part of Sub-Phase D (agents 53-55), since `resolveEncounters.ts`/`resolveCombatChoice.ts`/`initiateCombat.ts` live in `src/ships/` and were assigned to that sub-phase's own Simulation Core row, requiring `CombatEncounter`/`EncounterResult`/`CombatResolution`/`EncounterResolution` to exist there first. Only Presentation and Phase Integration remain here.

**See Sub-Phase D's own retroactive correction note above (2026-08-03)** — `resolveHazard()`'s failure-cost-band lookup, part of this sub-phase's own Simulation Core (ported in D), had a fractional-boundary gap. Fixed in lockstep in TS and C#; doesn't change anything Presentation/Phase Integration built here.

- [x] **Schema.** `CombatEncounter`, `EncounterResult` (+ `EncounterType`), `CombatResolution`, `EncounterResolution`, encounter-type weight table, hazard cost-curve table, trigger-chance constants — all ported in `agent-53-unity-ships-travel-schema.md`. `PendingCombat` is a TypeScript presentation-layer type (`src/presentation/shipsState.ts`, not `src/data/types/`), not core schema — Agent 58 (this sub-phase's own Presentation agent) defines its own Unity equivalent if the pending-combat indicator UI needs one.
- [x] **Simulation Core.** `resolveEncounters.ts`/`resolveCombatChoice.ts`/`initiateCombat.ts` ported and confirmed against this area's own test lens in `agent-54-unity-ships-travel-simulation-core.md`: the pending→resolved deferred-combat mechanism, opponent threat rolled once at detection never re-rolled at resolution, cargo never forfeited on any outcome (`ResolveCombatChoice`'s flee/lose branches both carry `voyage.Cargo` through to the retreat voyage unchanged).
- [x] **Parity Validation.** `resolveEncountersCases`/`resolveCombatChoiceCases`/`initiateCombatCases` in `agent-55-unity-ships-travel-parity-validation.md` cover the deferred-combat pending state, the retreat-voyage mechanism (flee and lose both produce one), and real-content encounter resolution against a real galaxy planet's producible resources.
- [x] **Infrastructure Adapters.** None needed — reuses Sub-Phase D's own `MarketState`/`Inventory` directly.
- [x] **Presentation** (`agent-63-unity-encounters-combat-presentation.md`). `ShipsState.PendingCombatEntry`/`PendingCombats` (new). `ShipsPanel.ResolveArrival` now opts into real encounter/combat detection (passing `destinationPlanet`/real content `Resources`), applies Trade/Discovery/Hazard outcomes to the real `Wallet`/`Inventory` via a new `ApplyEncounter`, and records any pending combat. New `ResolveCombat(combatEncounterId, choice)` trigger method resolves an Attack/Flee choice via `CombatChoiceResolver`, surfaced in `Refresh()` as a real pending-combat list with Attack/Flee buttons. `ShipsPanel`'s constructor gained `Inventory` (Discovery-encounter awards) and an optional `RandomFn` test-injection seam (mirrors `PlanetOwnershipState.SetSaveSystem`'s own seam pattern), defaulting to real `System.Random` in play. A won combat leaves the ship at its new location with no automatic return-voyage UI — an explicit, honest scope limit; only the automatic flee/lose retreat voyage demonstrates a return trip.
- [x] **Phase Integration** (`agent-64-unity-encounters-combat-integration.md`). Confirmed, via a fixed random sequence injected through the new test seam, that a real Trade Opportunity encounter credits the real Wallet by its exact rolled amount, and that a detected pending combat can be fled through the real UI wiring, producing a real automatic retreat voyage. No equivalent to Sub-Phase E's own found-and-fixed integration gap existed here — Agent 63 opted into `ArrivalResolver`'s additive-optional parameters directly at the only call site that needed them. Full sweep: `dotnet test` 743/743 (unchanged — no new `ProfitableCore` code), Unity EditMode 58/58, Unity PlayMode 2/2, `npm test` 687/687. **Migration Phase 2 (Sub-Phases A-F) is complete.**

---

## Post-completion audit (2026-08-04) — independently re-audited, not just trusted from Agent 64's own claim

Migration Phase 2's original completion claim (Agent 64, above) was independently re-verified in a follow-up session, using the same standard as a Phase 1 audit run just before it: re-run every real test suite directly rather than trusting prior counts, re-check every ported tier/breakpoint/threshold comparison for the fractional-boundary risk class, re-confirm the web/Electron build's own zero-footprint claim via a real diff, and investigate anomalies found along the way rather than working around them. Real findings, not just confirmation:

- **Tooling gap, disclosed rather than silently absorbed:** `profitable-unity-migration-gdd.md` §1 names Unity MCP as this migration's intended tooling ("Editor-driven work via MCP tools, not hand-edited scene/prefab files"). It was never actually set up for any part of this migration — no MCP server config exists anywhere in this repo. Agent 35's own contract already disclosed this at the time ("this project has no Unity MCP tooling available this session... its absence here is a real environment gap, not a design choice"), and every sub-phase since has continued the same real, working substitute: C# runtime scene construction (`MvpLoopBootstrap.cs`, each panel script) plus one `-executeMethod` scene-baking script (`Assets/Editor/SceneBuilder.cs`) instead of interactive Editor authoring.
- **A third fractional-boundary bug**, same class as `getTierColor()`/`penaltyCurve.ts` (both already documented and fixed in `profitable-mvp-gdd.md`): `HazardFailureCostCurve`'s band lookup in `resolveEncounters.ts`/`ResolveEncounters.cs`, found specifically during this audit — see Sub-Phase D's own retroactive correction note above for the full account. All three bugs now share the same fix pattern, regression tests in both TS and C#, and a retroactive correction note in their respective GDDs (`profitable-mvp-gdd.md` for the first two, `profitable-travel-encounters-gdd.md` and this checklist for the third).
- **A starting-credits porting error**: `MarketState.StartingCredits` (Unity) had drifted to 100 instead of the real TS source's 500 (`tradingState.ts`'s `STARTING_CREDITS`) — a genuine "port, don't redesign" violation the original per-sub-phase Parity Validation passes never caught, since it's a Presentation-layer constant, not a Simulation Core formula those passes exercise. Corrected to 500, matching the real source exactly. 500 is now logged in `docs/profitable-alpha-tuning-values.md` for the first time — never previously documented or tuned there — and accepted as the working value without further tuning, since it was implicitly validated by the already-completed feel-tuning playtest pass (no playtester ever flagged ship affordability as an issue while this value was live in the TS/Electron build).
- **Three presentation-completeness gaps closed**, found by comparing the real Unity build's actual reachable UI against the real TS alpha feature-by-feature, not by re-reading prior claims:
  - Refine/Craft recipe selectors — `RefinePanel.cs`/`CraftPanel.cs` were hardcoded to one recipe each since Phase 1; now list the real roster (10 refining, 13 general crafting — ship components stay excluded, still out of scope) as directly-actionable rows, mirroring `RefineScene.ts`/`CraftScene.ts`'s own real recipe-lock fix and this Unity build's own `ShipsPanel` shipyard-listing pattern.
  - Map-based travel — `MapPanel.cs` was read-only text; `ShipsPanel.cs`'s travel logic was hardcoded to one fixed `StartingPlanet <-> SecondaryDestinationPlanet` route. `ShipsPanel.InitiateVoyageTo` now generalizes voyage initiation to any of the real galaxy's 50 planets, and `MapPanel` renders a real, clickable Travel button per reachable planet.
  - Market buy — `MarketPanel.cs` only ever sold. Real Listing creation and purchase (planet + global) added, including `purchaseListing()`'s self-trade check, proven end-to-end (list something as the player, get correctly rejected buying it back, still successfully buy a real seed listing in the same test).
- **Confirmed still absent, via direct audit — all pre-existing documented scope cuts, not newly discovered gaps**: ship component crafting, ship assembly/component-swap UI, ~~Scanner UI entirely (purchase/scan)~~, the debug/tuning panel, and won-combat return-voyage UI (only the automatic flee/lose retreat voyage demonstrates a return trip — see Sub-Phase F's own Presentation note above). **Update (2026-08-04, later same day): Scanner UI is no longer absent** — see the Follow-ups section below, where building it closed the tracked discovery-gate line. The other three items in this list are still accurate.
- **Housekeeping**: a stray, untracked 2.3GB nested git clone at `unity/ProfitableUnity/profitable/` (a full duplicate of this repo, apparently from Unity Hub's project list pointing "Add project from disk" at the wrong folder) was found — a live Unity Editor process was even discovered pointed at it mid-audit — and removed once confirmed to contain no unique work. A `.gitignore` rule (`unity/ProfitableUnity/profitable/`) was added so it can't silently recur.
- **Web/Electron build reconfirmed untouched**: every Unity-tagged commit across the entire migration (Phase 1 through this audit) diffed directly — zero footprint outside `unity/`/`docs/` except `.gitignore`/`package.json` (both purely additive, one commit each) and `scripts/parityHarness.ts` (imports and calls `src/simulation/*` read-only, never modifies it).

**Current verified counts (2026-08-04):** `dotnet test` 748/748, `npm test` 688/688, Unity EditMode 76/76, Unity PlayMode 5/5 (stable across repeated runs). All work described above is committed and pushed to `origin/main`.

## Follow-ups (not yet scheduled, tracked here so they don't only live in code comments)

- [x] **Revisit travel's discovery-gate once Scanner UI is built.** Closed (2026-08-04), together with building Scanner UI itself — the two were never separable (the gate had nothing real to gate against until a real way to discover a planet existed in Unity). Scanner purchase now lives in `ShipsPanel.cs` (mirrors `ShipyardScene.ts`'s own "scanners for sale alongside ships" placement) via the already-ported, already-parity-tested `ScannerPurchaser`; scan now lives in `MapPanel.cs` (mirrors `TradeMapScene.ts`'s own placement) via the already-ported `ScanPerformer`, with real discoveries written back through a new `GalaxyState.MarkDiscovered`. `MapPanel`'s planet list (not just the travel-destination sub-list) is re-gated to `Discovered` planets only, matching `TradeMapScene.ts`'s own `getDiscoveredPlanets()`-scoped display exactly — the "every planet is a valid destination" stopgap is gone. Verified via real Unity click-through (purchase scanner, scan, confirm newly-discovered planets gain a real Travel button, confirm still-undiscovered planets stay excluded), not by calling the underlying C# methods directly — same standard as every prior gap closure in this section, documented here rather than in a new numbered agent doc (matching how the Refine/Craft/Map/Market gaps just above were closed and recorded). Unity EditMode 76→82, Unity PlayMode 5→6.
- [x] **Citadels removed from alpha scope (2026-08-04, later same day than the above).** Not a follow-up in the sense of unfinished work — the opposite: a fully-finished, fully-tested system (TS and Unity/C# both) was deliberately cut. `dotnet test` reconfirmed at 729/729 and `npm test` at 666/666 post-removal (both down from the counts above by exactly the removed Citadel-specific cases). Unity EditMode/PlayMode were not independently re-run in this environment. See Sub-Phase E's own retroactive-removal note above and `docs/functional-agents/planet-ownership.md` for the full account.

---

## Explicitly excluded / deferred (not part of this checklist)

- **Universe / Multiplayer** (`universe.md`) — fully designed, zero code on the TypeScript side, deliberately unscheduled until the project moves toward being a packaged app. Nothing to port yet on either side.
- **UI/UX polish parity** (debug/tuning panel, onboarding tooltip flow, settings screen, tier-color accessibility pass — Alpha Section 4) — not named in any sub-phase above because these are Phaser-specific presentation features, not simulation logic; whether/how they get a Unity equivalent is a presentation-design question for whoever builds each sub-phase's Presentation role, not a porting task with a TypeScript source of truth to translate.
- **Electron packaging** (Alpha Section 5) — has no Unity analog; Unity has its own native build/packaging pipeline, out of scope for this migration checklist entirely.

---

## Definition of "Unity Alpha Phase 2 Complete"

- Every sub-phase above (A through F) has a completed six-role roster (or five, where no new Infrastructure Adapter was needed) with its own Parity Validation and Phase Integration both reporting success, per `profitable-unity-migration-gdd.md` §6's non-negotiable numeric-parity standard.
- A player can complete the full loop — gather → refine → craft → trade → travel → crew → ships → colonize a planet → combat — entirely inside the Unity build, using the real, existing content (`content/*.json`, consumed as-is, never re-authored). (Originally "colonize/claim a planet" — the claim/Citadel half was cut, see the retroactive-removal note above.)
- Every sub-phase's parity corpus still passes together, in one run, at the end — proving later sub-phases didn't silently regress earlier ones.
- The existing web/Electron TypeScript build (`src/`, `content/`) is provably untouched throughout — it remains live and deployable the entire time, per the GDD's own "not a cutover" framing.
- All TS-side prerequisites are already closed as of this checklist's current revision: Crew's documentation gap (§0) and Planet Ownership's cargo-storage question (repurposed into a Level 2 repair tier, Sub-Phase E) are both resolved — nothing left blocking any sub-phase above on the TypeScript side.
